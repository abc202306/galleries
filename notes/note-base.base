filters:
  and:
    - up.containsAny(link("notes", "notes"))
views:
  - type: cards
    name: Cards
    order:
      - file.name
      - seealso
      - url
    sort:
      - property: ctime
        direction: DESC
    image: note.cover
    cardSize: 100
